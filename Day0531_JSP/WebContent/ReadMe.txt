EL(Expression Language)(표현언어)
${값}
JSP 페이지 내에서 자바코드의 값을 쉽게 출력하기 위해서 사용
<%= %> 표현부  와 비슷한 역할
왜 언어?
${값(연산이 가능)}
표현언어 사용법에 대해서 공부합시다.

JSTL (JSP Standard Tag Library)
: JSP에서 자주 사용되는 유용한 태그들을 모아놓은 library
core,format
